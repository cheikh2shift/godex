version: "3.8"

services:
  ollama-proxy:
    image: nginx:alpine
    container_name: ollama-proxy
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - godex-network
    depends_on:
      - godex

  godex:
    build:
      context: .
      platform: linux/amd64
    image: godex-sandbox
    container_name: godex
    environment:
      - HOME=/godex-home
    volumes:
      - .:/workspace
      - godex-config:/godex-home/.godex
    working_dir: /workspace
    stdin_open: true
    tty: true
    restart: "no"
    networks:
      - godex-network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
      - /run
    user: "1000:1000"
    cap_drop:
      - ALL
    cap_add:
      - NET_RAW
      - NET_BIND_SERVICE
    ulimits:
      nproc: 512
      nofile:
        soft: 256
        hard: 256

networks:
  godex-network:
    driver: bridge
